{
  "name": "hawker-heroes",
  "version": "1.0.0",
  "private": true,
  "description": "2-player co-op hawker-centre serving game (Phaser).",
  "scripts": {
    "test": "node test/smoke.js",
    "stage": "node scripts/stage-deploy.mjs",
    "deploy": "node scripts/stage-deploy.mjs && wrangler deploy",
    "deploy:dry": "node scripts/stage-deploy.mjs && wrangler deploy --dry-run"
  },
  "devDependencies": {
    "puppeteer": "^25.1.0",
    "wrangler": "^4.100.0"
  }
}
